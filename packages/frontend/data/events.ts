import type { ParseKeys } from 'i18next';

/** The demo Home Brief's paragraphs, as translation keys — resolved with `t()` where the brief renders. */
export const BRIEF_PARAGRAPH_KEYS: ParseKeys[] = ['activity.brief.paragraph1', 'activity.brief.paragraph2', 'activity.brief.paragraph3'];
