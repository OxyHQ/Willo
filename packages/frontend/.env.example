# Oxy platform API base URL.
EXPO_PUBLIC_OXY_API_URL=https://api.oxy.so

# Not yet provisioned (requires an ops deploy to OxyHQ/oxy). Leave blank
# until then — sign-in still works without it, just not the cross-app
# device sign-in (QR/approval) flow.
EXPO_PUBLIC_OXY_CLIENT_ID=
EXPO_PUBLIC_OXY_AUTH_REDIRECT_URI=
